import { defineConfig } from 'vite'

export default defineConfig({
    //base: '/homographic-music-visualizer/',
    base: './',
    build: {
        outDir: './dist',
        emptyOutDir: true,
    },
})
